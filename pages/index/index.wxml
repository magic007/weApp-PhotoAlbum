
<view class="page">

    <view class="page__bd">
        <view class="weui-grids">
            <block wx:for="{{diaryList}}" wx:key="diaryItem">
                <navigator url="/pages/index/detail/detail?id={{item.objectId}}" class="weui-grid" hover-class="weui-grid_active">
                    <image class="weui-grid__icon" src="{{item.pic._url}}" />
                    <!--<view class="weui-grid__label">{{item.title}}</view>-->
                </navigator>
            </block>
        </view>
    </view>
</view>

